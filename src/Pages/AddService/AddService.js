import React from 'react';
import useTitle from '../../useTitle';

const AddService = () => {
    useTitle("Add Services")
    return (
        <div>
            
        </div>
    );
};

export default AddService;